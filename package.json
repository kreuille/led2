{
  "name": "led2",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test": "npm run build && node scripts/validate.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sharp": "^0.35.3",
    "typescript": "^5.7.2",
    "vite": "^6.0.5"
  },
  "dependencies": {
    "@jaames/iro": "^5.5.2"
  }
}
